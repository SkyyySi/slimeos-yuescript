--[[
submodules = {
	"bars"
	"boxes"
	"layouts"
	"lib"
	"settings"
	"widgets"
}

module = {}

for submodule in *submodules
	module[submodule] = require("slimeos.#{submodule}")

module
--]]

import "slimeos.lib.util.macro" as { $ }

$NAMESPACE

